export {};

  let gravity = 1.2,
  friction = .75;


   function getNewPlayerPosition(player) {

    // let currentPlayerVelocityX = player.velocity.x;
    // let currentPlayerVelocityY = player.velocity.y;
    //
    // let currentPlayerPositionX = player.position.x;
    // let currentPlayerPositionY = player.position.y;
    //
    //
    // let newPlayerVelocityY =
    // let newPlayerVelocityY =
    //
    //
    // let newPlayerPositionX = currentPlayerPositionX +
    // let newPlayerPositionX =




  }


  function getNewBulletPosition(bullet) {

    // let currentBulletVelocity = { x: , y: };
    // let newBulletVelocity = { x: , y: };
    //
    // let currentBulletPosition = { x: , y: };
    // let newBulletPosition = { x: , y: };

    // bullet.velocity.y += this.gravity - .5;
    // bullet.position.x += bullet.velocity.x;
    // bullet.position.y += bullet.velocity.y;




  }
